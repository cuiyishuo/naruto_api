<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!--通过namespace绑定上面创建的接口类，文件名就是【数据对象类名+Mapper.xml】-->
<mapper namespace="com.solplatform.mapper.ProjectMapper">

    <insert id="addProject" parameterType="com.solplatform.entity.ProjectEntity" useGeneratedKeys="true"
            keyProperty="id" keyColumn="id">
        insert into project (project_name,create_time,update_time)
        values (#{projectName},now(),now())
    </insert>

    <!--通过set标签，哪个字段有值更新哪个字段-->
    <update id="modifyProject" parameterType="com.solplatform.entity.ProjectEntity">
        update project
        <set>
            <if test="projecrName != null">
                project_name=#{projectName},
            </if>
            <if test="yn != null">
                yn=#{yn}
            </if>
        </set>
        where id=#{id}
    </update>

</mapper>